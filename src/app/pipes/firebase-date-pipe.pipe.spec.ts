import { FirebaseDatePipePipe } from './firebase-date-pipe.pipe';

describe('FirebaseDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FirebaseDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
